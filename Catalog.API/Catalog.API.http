@hostAddress = http://localhost:4024

### List all catalog items (EventId, EventName, Price, AvailableSeats).
# Use this to watch AvailableSeats drop after a booking is confirmed and synced.
GET {{hostAddress}}/items
Accept: application/json
